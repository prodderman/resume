mixin info({ title, date, place, text, color })
  .info(class=color ? ` info_color_${color}` : '')
    .info__header
      .info__title=title
      .info__date=`${date}`
      .info__place=place
    p.info__text=text
